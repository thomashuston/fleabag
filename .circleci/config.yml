version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run
  nightly:
    triggers:
      - schedule:
          cron: '0,15,30,45 * * * *'
          filters:
            branches:
              only:
                - master
    jobs:
      - cypress/run
